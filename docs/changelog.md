# Changelog

## 文档信息

- 版本：v1.0
- 更新时间：2026-03-26 17:08:59
- 作用：记录当前项目已完成事项、已知问题/风险、下一步计划，以及新对话可复用的 memory。

## 1) 目前已完成

- 完成需求说明书初版：`exam-platform-requirements-spec.md`。
- 基于你的多轮确认，完成需求说明书升级：
  - `exam-platform-requirements-spec-v1.1.md`
  - `exam-platform-requirements-spec-v1.2.md`
- 已将首页看板关键规则纳入 `v1.2`：
  - 平台管理员首页单卡展示：`今日预计考试人数 / 当前考试登录人数`
  - 所有豆腐块右上角 `!` 图标，悬停/点击可查看口径定义
  - 成绩可见规则：`评分完成` 且 `成绩发布开关=开`
  - 学生结果显示：未发布时展示“考试结果尚未公布”
- 完成 UI/前端设计规范：`design-system.md`（适配小学/初中考试培训系统）。
- 完成偏好沉淀：`user.md`（路径、技术栈、文档版本化规则、执行规则）。

## 2) 已知问题 / 风险 / Bug 记录

- Markdown 格式层面（非业务）存在规范性告警（如标题前后空行、表格格式等），不影响当前需求逻辑，但后续若接入 CI 需统一格式。
- Windows 中文路径在个别脚本读取时可能触发编码/路径兼容问题（已通过读取方式规避）。
- 需求迭代频繁，若不严格遵守“版本叠加更新”容易出现口径漂移。

## 3) 下一步建议

- 先把 `v1.2` 拆为“可开发任务清单”（前端/后端/测试）。
- 先落地前端基础壳与核心通用组件：
  - `MetricCard`（含口径 `!`）
  - `DefinitionTooltip`
  - `ExamAnswerCard`
  - `UploadAnswerPanel`
  - `ScoreVisibilityState`
- 后端优先实现：
  - 首页看板指标接口与口径对齐
  - 评分完成+发布开关的结果可见判定接口
  - 场次实时状态与开关接口

## 4) New Chat Memory（可直接喂给新对话）

可复制以下内容作为新对话上下文：

1. 项目根目录：`C:\Apps\TraeOwnProject\examSystem`。  
2. 文档与代码目录：`docs/`、`frontend/`、`backend/`。  
3. 技术栈：前端 `Vue`，后端 `Java`，数据库 `MySQL`，部署 `Docker`。  
4. 文档更新必须版本化：保留源文件，复制新版再改（`v1.0 -> v1.1 -> v1.2`）。  
5. 当前需求基线文件：`docs/exam-platform-requirements-spec-v1.2.md`。  
6. UI 规范基线文件：`docs/design-system.md`。  
7. 首页关键规则：  
   - 平台管理员单卡展示：`今日预计考试人数 / 当前考试登录人数`；  
   - 所有豆腐块右上角有 `!`，悬停/点击显示口径定义；  
   - 成绩可见条件：`评分完成` 且 `成绩发布开关=开`，否则显示“考试结果尚未公布”。  
8. 学生考试主观题：附件上传作答（图片/文档/压缩包），支持预览与重传，实时保存。  

## 5) 版本更新规则（强制）

- 本文件后续更新采用叠加版本，不覆盖历史版本：
  - `changelog.md`（当前基线 v1.0）
  - 下一版：`changelog-v1.1.md`
  - 再下一版：`changelog-v1.2.md`
- 每次新对话开始时，先读取 `docs` 下最新版本的 changelog 再继续工作。
