# Changelog

## 文档信息

- 版本：v1.1
- 更新时间：2026-03-26 23:50:00
- 作用：记录当前项目已完成事项、已知问题/风险、下一步计划，以及新对话可复用的 memory。
- 对齐依据：`exam-platform-requirements-spec-v1.3.md`、`design-system-v1.1.md`、`tech-stack.md`

## 1) 目前已完成

- 完成需求说明书升级：`exam-platform-requirements-spec-v1.3.md`。
- 完成 UI 设计规范升级：`design-system-v1.1.md`。
- 完成技术栈清单更新：`tech-stack.md`。
- 确认后续前端开发必须遵循最新 `design-system` 文档，界面实现优先匹配 UI 规范版本。
- 确认后续编程开发必须严格遵循 `tech-stack.md`，框架、库、部署与工程化选择以该清单为准。
- 继续沿用需求版本化规则：每次需求变更先更新需求文档，再开展开发实施。
- 当前仍保留已完成的前端管理员模块与架构文档版本化成果：
  - 平台管理员前端模块已完成假数据版并可路由跳转。
  - 架构文档已完成 `architecture-v1.2.md` 版本迭代。

## 2) 已知问题 / 风险 / Bug 记录

- Markdown 格式层面（非业务）存在规范性告警（如标题前后空行、表格格式等），不影响当前需求逻辑，但后续若接入 CI 需统一格式。
- Windows 中文路径在个别脚本读取时可能触发编码/路径兼容问题（已通过读取方式规避）。
- 需求、设计、技术栈文档均进入版本化节奏后，若未同步读取最新版本，容易出现实现口径漂移。

## 3) 下一步建议

- 先把 `v1.3` 拆为“可开发任务清单”（前端/后端/测试）。
- 前端实现应优先对齐 `design-system-v1.1.md`，先完成公共组件、页面布局和交互反馈统一。
- 编程实现前先核对 `tech-stack.md`，避免在框架、路由、状态管理、UI 组件库上出现偏离。
- 后端优先实现：
  - 首页看板指标接口与口径对齐
  - 评分完成+发布开关的结果可见判定接口
  - 场次实时状态与开关接口

## 4) New Chat Memory（可直接喂给新对话）

可复制以下内容作为新对话上下文：

1. 项目根目录：`C:\Apps\TraeOwnProject\examSystem`。  
2. 文档与代码目录：`docs/`、`frontend/`、`backend/`。  
3. 技术栈：前端 `Vue`，后端 `Java`，数据库 `MySQL`，部署 `Docker`。  
4. 文档更新必须版本化：保留源文件，复制新版再改。  
5. 当前需求基线文件：`docs/exam-platform-requirements-spec-v1.3.md`。  
6. UI 规范基线文件：`docs/design-system-v1.1.md`。  
7. 技术栈基线文件：`docs/tech-stack.md`。  
8. 前端开发必须遵循最新 `design-system` 文档。  
9. 编程开发必须严格遵循 `tech-stack.md`。  
10. 首页关键规则：  
   - 平台管理员单卡展示：`今日预计考试人数 / 当前考试登录人数`；  
   - 所有豆腐块右上角有 `!`，悬停/点击显示口径定义；  
   - 成绩可见条件：`评分完成` 且 `成绩发布开关=开`，否则显示“考试结果尚未公布”。  
11. 学生考试主观题：附件上传作答（图片/文档/压缩包），支持预览与重传，实时保存。  

## 5) 版本更新规则（强制）

- 本文件后续更新采用叠加版本，不覆盖历史版本：
  - `changelog.md`（当前基线 v1.0）
  - `changelog-v1.1.md`（本次更新版本）
  - 下一版：`changelog-v1.2.md`
- 每次新对话开始时，先读取 `docs` 下最新版本的 changelog 再继续工作。
